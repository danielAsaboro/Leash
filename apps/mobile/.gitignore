# Isolated Expo project — its own node_modules (not part of the root npm workspace).
node_modules/

# Expo / Metro
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `expo prebuild` (Continuous Native Generation).
/ios/
/android/

# misc
*.log
.DS_Store
