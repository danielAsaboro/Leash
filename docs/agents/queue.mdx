---
title: The broker
description: A cross-process priority queue in front of qvac serve that serializes per-model inference and survives client disconnects.
---

Leash's `leash-broker` is a standalone reverse proxy on `:11436` that fronts `qvac serve` on `:11435`. It is the single chokepoint that serializes inference per model, prioritizes across models, and keeps long decodes alive when a client walks away.

```bash
npm run broker   # listens on :11436 → forwards to serve :11435
```

## Why it exists

The SDK's in-process request registry **queues** concurrent same-alias requests (FIFO, default `maxConcurrentPerModel: 1`, `maxQueueDepthPerModel: 64`) instead of the old `500 "Cannot set new job…"` rejection. Each OS process has its own registry, so the broker still serializes **cross-process** — a separate web-chat process and a compaction child each maintain independent queues and cannot coordinate without the broker.

<Warning>
  Every serve client is a **separate OS process** — the web chat route, the compaction / dream / research children, the watcher. An in-process mutex can't coordinate them. The broker is the only place that can.
</Warning>

<CardGroup cols={2}>
  <Card title="Per-alias serialization" icon="lock">
    Concurrency **1 per model** (alias). At most one request in flight per alias; the rest wait.
  </Card>
  <Card title="Different aliases run parallel" icon="layer-group">
    Serialization is **per alias**, so `qwen3-4b` and an embeddings model decode at the same time — they don't share a context.
  </Card>
  <Card title="Cancel on disconnect" icon="shield-halved">
    On client disconnect the broker **aborts the upstream fetch**, which triggers the serve's cancel-bridge (`cancel({ requestId })`) — freeing the GPU mid-decode. Only bytes already in flight are drained; the full decode no longer runs to completion.
  </Card>
  <Card title="No upstream timeout" icon="hourglass">
    `bodyTimeout: 0` — the broker never body-times-out a slow decode.
  </Card>
</CardGroup>

## How a request flows

<Steps>
  <Step title="Read the alias">
    The broker parses the JSON body's `model` field. Non-JSON or model-less traffic (the model list, multipart audio) passes through **unqueued** — those endpoints don't collide on a chat/vision context.
  </Step>
  <Step title="Acquire the alias slot">
    If the alias is idle, the request goes straight through. If busy, it waits in that alias's queue, ordered by priority + aging (see [Priority & aging](/agents/queue-priority)).
  </Step>
  <Step title="Forward with abort signal">
    The upstream `fetch` is tied to a per-request `AbortController`. If the client disconnects mid-decode, the broker aborts the fetch — the serve's cancel-bridge fires `cancel({ requestId })` and the GPU is freed. The same abort is forwarded to the hypha overflow path.
  </Step>
  <Step title="Stream (or drain in-flight bytes)">
    While the client is connected, upstream bytes stream straight through (respecting backpressure). If the client disconnects, the broker aborts the upstream — only bytes already buffered in flight are drained; the decode itself is cancelled at the serve.
  </Step>
  <Step title="Release the slot">
    On completion (or upstream error → `502`), the slot is released and the next waiter — picked by aged priority — is woken.
  </Step>
</Steps>

## Cancel on disconnect

<Note>
  Since `@qvac/sdk@0.13.1`, mid-decode cancellation is safe — aborting a llama.cpp decode no longer wedges the GPU. The broker's governing principle: **abort the serve on client disconnect** so the GPU is freed immediately. The serve ships a cancel-bridge: each completion is armed with `req.bindCancel(result.requestId)`; on `req.once('close')` it fires `cancel({ requestId })`. If a client gives up *while still queued*, the broker simply frees the slot without starting a decode — no tokens are burned on an abandoned request.
</Note>

## Scheduling lineage

The broker is **non-preemptive priority scheduling**: the serve is a non-preemptible black box, so the broker schedules *which request enters next*, never interrupting one in flight. The design borrows from OS MLFQ (multi-level feedback queue) with priority + aging.

<Tip>
  The LLM-serving papers — Orca, vLLM, FastServe, Sarathi — diagnose the same contention but assume **engine control** (token-level preemption, paged KV) that a black-box serve doesn't expose. The broker schedules at the request boundary instead.
</Tip>

## Diagnostics

`GET /__broker/stats` returns live state — the upstream, total served, overflow counters, and per-alias `{ busy, queued }` depth.

## Configuration

| Env var | Default | Purpose |
| --- | --- | --- |
| `LEASH_BROKER_PORT` | `11436` | Listen port |
| `LEASH_BROKER_UPSTREAM` | `http://127.0.0.1:11435` | The serve to forward to |
| `LEASH_BROKER_AGING_MS` | `8000` | Anti-starvation aging interval (see priority page) |

## Related

<CardGroup cols={2}>
  <Card title="Priority & aging" icon="arrow-up-wide-short" href="/agents/queue-priority">
    Cross-alias priority tiers, aging, and mesh overflow.
  </Card>
  <Card title="Runtime" icon="gears" href="/agents/runtime">
    Where the broker sits in Leash's process model.
  </Card>
  <Card title="Ports & processes" icon="network-wired" href="/reference/runtime-ports-and-processes">
    The full port map: web, serve, broker, hypha.
  </Card>
  <Card title="Debugging" icon="bug" href="/help/debugging">
    Diagnosing queue stalls and cancel latency.
  </Card>
</CardGroup>
