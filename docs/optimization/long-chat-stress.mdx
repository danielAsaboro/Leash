---
title: "Long chat stress"
description: "The 30-50 turn chat API stress run used to catch routing, tool, and compaction regressions."
sidebarTitle: "Long chat stress"
---

The long-chat stress runner exercises the product path users actually use: repeated `POST /api/leash/chat` calls with normal chat messages. It mixes quick turns, tool turns, broker turns, health safety, file search, and continuity markers so the route has to survive a long conversation instead of one idealized request.

## Scenarios covered

| Scenario | What it exercises |
| --- | --- |
| Quick direct | No-model direct chat path. |
| No-tool-word guard | Ensures lightweight prompts that mention tools do not force the full agent. |
| Files date | Registered `bash` through chat and goal-run evidence. |
| File-finder metadata | Skill metadata answer without disk search. |
| File-finder search and no-result | Bounded read-only local search. |
| Context broker | `context_run` graph search. |
| Memory broker | Memory recall/read broker. |
| Tasks broker | Task list/read broker. |
| Daily paper broker | `understory_today` broker action. |
| Health route | Read-only health context with non-diagnostic answer. |
| Continuity turns | Long-tail chat after previous tool calls. |

## Latest 30-turn mixed run

Latest report: `logs/stress-long-chat-mrqdvodw.json`.

| Metric | Result |
| --- | --- |
| Completed turns | 30/30 |
| Routes observed | `chat`, `direct`, `files`, `health` |
| Skills observed | `file-finder`, `memory-keeper` |
| Tools observed | `bash`, `context_run`, `memory_run`, `recall`, `search_graph`, `tasks_run` |
| Total wall time | 117.518 s |
| p50 turn latency | 141 ms |
| p95 turn latency | 20.265 s |
| Max turn latency | 41.196 s |

The max latency was the real health-model turn. The final 18 continuity turns, after all earlier tools and model routes, stayed between 54 ms and 188 ms.

## Scenario averages

| Scenario | Average |
| --- | --- |
| Quick direct | 1.019 s |
| No-tool-word guard | 45 ms |
| Files date | 9.228 s |
| File-finder metadata | 93 ms |
| File-finder search | 20.213 s |
| File-finder no-result | 20.265 s |
| Context broker | 12.411 s |
| Memory broker | 264 ms |
| Tasks broker | 4.850 s |
| Daily paper broker | 3.629 s |
| Health route | 41.196 s |
| Continuity after tools | 2.167 s |
| Long-tail continuity | 119 ms average; 54-188 ms range |

## Before and after

| Area | Before | After |
| --- | --- | --- |
| File-finder live probe | Around 17 minutes in the worst probe. | 20.213-20.265 s in the latest mixed run. |
| Files `date` | Around 168 s before the direct registered-tool path. | 9.228 s including current registry/tool startup. |
| Broker turns | Around 10-11 s through full agent routing. | 264 ms to 12.411 s, depending on action and cache state. |
| Health safety | Around 94 s through model routing. | 41.196 s on the current real health-model path. |
| Long-tail continuity | Around 16-62 ms after direct routing work. | 54-188 ms after the current tool-heavy conversation. |

Earlier reports used while tightening the system: `logs/stress-long-chat-mqmm592x.json` and `logs/stress-long-chat-mqmm4nhn.json`.

## Acceptance bar

A run is only useful if it proves the chat surface held together. The runner checks HTTP success, `text/event-stream`, conductor metadata, observed routes, observed skills, and observed tools. Internal helper scripts can isolate failures, but the long stress run is the product-path proof.
