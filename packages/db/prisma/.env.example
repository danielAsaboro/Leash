# Copy this to `.env` (gitignored). Prisma CLI (migrate/generate) reads it.
# The `file:` path is relative to this schema directory → packages/db/prisma/newsroom.db.
# The runtime client opens the same absolute path explicitly (src/index.ts), so cwd never
# matters at runtime.
DATABASE_URL=file:newsroom.db
